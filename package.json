{
  "name": "delhi-metro-dashboard",
  "version": "1.0.0",
  "description": "Delhi Metro Data Visualization Dashboard",
  "main": "api.py",
  "scripts": {
    "start": "streamlit run app.py --server.port $PORT --server.address 0.0.0.0",
    "build": "pip install -r requirements.txt"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}